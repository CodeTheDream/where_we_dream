<% title 'All comments on ' + @school.name %>
<h2><%= link_to @school.name, school_path(@school) %></h2>
<h3>Comments · <%= @school.comments.count %></h3>
<%= render 'comments/form' if logged_in? %>
<%= render 'comments/index' %>
